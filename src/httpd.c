
#include "httpd.h"

#include <lwip/api.h>

/*
void netconn_callback(struct netconn *,enum netconn_evt,u16_t len)
{

}

void start(void)
{
	struct netconn *conn;


	conn = netconn_new_with_callback(NETCONN_TCP, netconn_callback);
	netconn_set_nonblocking(conn, 1);

}*/
